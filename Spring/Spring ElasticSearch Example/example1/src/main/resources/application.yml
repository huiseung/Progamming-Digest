spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    username: root
    password: gmltmd75
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true

    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  data:
    elasticsearch:
      repositories:
        enabled: true